filebeat.inputs:
- type: docker
  combine_partial: true
  containers:
    paths:
    - '/usr/share/dockerlogs/data/*/*.log'
    stream: "all"
    ids:
      - "*"
  exclude_files: ['\.gz$']
  ignore_older: 10m

processors:
# decode the log field (sub JSON document) if JSON encoded, then maps it's fields to elasticsearch fields
- decode_json_fields:
    fields: ["log","message"]
    target: ""
  # overwrite existing target elasticsearch fields while decoding json fields    
    overwrite_keys: true
- add_docker_metadata:
    host: "unix:///var/run/docker.sock"

filebeat.config.modules:
  path: ${path.config}/modules.d/*.yml
  reload.enabled: false

# setup filebeat to send output to elasticsearch
output.elasticsearch:
  hosts: ["172.20.20.207:9200"]
#   index: "testapp-%{[beat.version]}-%{+yyyy.MM.dd}"

# setup.template.name: "testapp"
# setup.template.pattern: "testapp-*"
# setup.dashboard.index: "testapp-*"

# setup.kibana:
#   host: "172.20.20.207:5601"

# Write Filebeat own logs only to file to avoid catching them with itself in docker log files
logging.level: debug
logging.to_files: false
logging.to_syslog: true
